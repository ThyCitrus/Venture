/**
 * Class to Scan buffers
 */
export declare class Scan {
    /** Buffer */
    private buffer;
    /** last line */
    private lastLine;
    /**
     * Constructor of Scan
     *
     * @param buffer
     */
    constructor(buffer: string);
    /**
     * Scan the buffer
     */
    reverseScan(regexp: RegExp, startLine: number, iterator: any): void;
    /**
     * Scan the buffer
     */
    scan(regexp: RegExp, iterator: any): void;
    /**
     * Count lines to the specified index
     */
    private countLinesTo;
}
